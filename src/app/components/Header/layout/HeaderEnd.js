import React from 'react';

import '../styles/HeaderEnd.scss';

const HeaderEnd = ({ children }) => {
    return(
        <div>
            {children}
        </div>
    );
}

export default HeaderEnd;