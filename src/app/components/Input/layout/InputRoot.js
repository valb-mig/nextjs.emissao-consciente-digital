import React from 'react';

import '../styles/InputRoot.scss';

const InputRoot = ({ children }) => {
    return(
        <div>
            {children}
        </div>
    );
}

export default InputRoot;